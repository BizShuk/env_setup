{
  "name": "Env Setup (Alpine)",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind",
  "workspaceFolder": "/home/shuk",
  "remoteUser": "shuk",
  "postCreateCommand": "echo 'Container ready! Workspace mounted at /workspace, project at ~/projects/env_setup'"
}
