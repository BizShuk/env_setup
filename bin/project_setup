#!/bin/bash

echo "" >> README.md
echo "" >> GEMINI.md
# ln -sf README.md AGENTS.md
# ln -sf README.md GEMINI.md
# ln -sf README.md CLAUDE.md

# Initialize documentation files if they don't exist
[ ! -f "README.todo" ] && echo -e "TODO:\n    ☐ " > README.todo
[ ! -f "README.prompt.md" ] && cat <<EOF > README.prompt.md
# Prompts

This file contains useful prompts for this project.
EOF


# .agents setup
mkdir -p .agents/rules
mkdir -p .agents/skills

if [ ! -f ".agents/settings.json" ]; then

cat <<EOF > .agents/settings.json
{
  "mcp": {
    "excluded": [
    ]
  },
  "mcpServers": {
  }
}
EOF

fi

# symbolic link
ln -sfh .agents .gemini
# ln -sfh .agents .agentss
# ln -sh .agents .claude

ln -sfh .gitignore .geminiignore


project_path=$(pwd)
project_name=$(basename "$project_path")

if [ -d "$HOME/projects/llm_plugin" ]; then
  ln -sfh "$(pwd)/.agents" "$HOME/projects/llm_plugin/projects/${project_name}"
fi


