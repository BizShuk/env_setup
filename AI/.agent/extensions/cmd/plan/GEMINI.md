# General Plan Mode

This is Planning mode extension. Commands are domain-based. If no command for the domain, then use general one.

NOTE: Strictly no modification on the work directory except file name pattern /^plan-.+.md/
