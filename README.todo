# TODO

- [x] 執行 `plans/2026-07-08-env-setup-structural-cleanup.md` — env_setup 結構整理 (truthful docs / 去重 / sanitization / scalability / bugfix) @2026-07-09_16:55:00 @Completed

## <structural-cleanup>

> 對應 `plans/2026-07-08-env-setup-structural-cleanup.md`。
> 體檢後新增項目: (a) `bin/system/system_info:8` BASE_DIR bug；(b) `bin/system/README.md` 標題錯置 (寫「安全性稽核」, 實為「硬體偵測」)；(c) `run.sh` 與 `bin/system/system_link` 之目標路徑分歧 (`./tmp/` vs `./config/`)；(d) 23 個未列入原 plan 的 `bin/` 入口 (見 `docs/bin_index.md`)；(e) `pkg/linux/*.md` 7 份 ubuntu 筆記需搬遷；(f) `bin/scan_target_network` 為第三個 network scanner；(g) `bin/system/brew_bundle_dump` 與 `bin/mac/mac_cleanup` 都硬編 Brewfile 路徑。

- [x] Phase 2 — 安全化 (sanitize) @2026-07-09_16:50:00 @Completed @<structural-cleanup>
    - `bin/bash/settings.sh` 移除 `passwd` / `email`；改由 `~/.config/env_setup/settings.private.sh` 提供 (已實作於 line 11-14)
    - 刪除 `bin/bytedance_setup.sh` (含個人 SSH/Git config 模板)
    - `.gitignore` 已含 `settings.private.sh`, `.bash_local`, `*.bak`, `log/`, `tmp/`, `**/.DS_Store`
    - `bin/bash/.bash_aliases` 之 6 條含 `TIKTOK`/`MINIMAX` token 的 `claudew*` / `claudem` alias 已改為引用 `~/.bash_local` 變數並在 `.bash_aliases` 內預配置；更新 `docs/notes/bash-local-aliases.md` 說明
- [x] Phase 4 — 結構去重與重組 (structural dedup) @2026-07-09_16:48:00 @Completed @<structural-cleanup>
    - 合併 `bin/system/system_link` → `run.sh`；目標路徑統一為 `./tmp/`
    - 建立 `bin/mac/_lib_audit.sh`；重構四個 audit 腳本 (`disk`, `launch`, `login`, `network_security`)
    - 整合三個 network scanner (`bin/scan_private_network`, `bin/scan_target_network`, `bin/system/network_topology_scan.sh`) → `bin/network/scan_network.sh --mode=private|target|topology|topology-no-scan`
    - 修正 `bin/system/system_info:8` BASE_DIR bug (已由 12e03fa 修復為 `BASE_DIR="$(dirname "$0")"`)
    - 刪除 `bin/goswitch` (硬編 `~/settings.sh` + 指向不存在之 `go_setup.sh`)
    - 刪除 `bin/claudew` (與 `bin/bash/.bash_aliases:126` 重複)
    - `bin/mac/*.sh` 統一加 `mac_` 前綴與 `.sh` 後綴 (例: `mac_cleanup` → `mac_cleanup.sh`); 重建根層便捷 symlink
    - `bin/system/brew_bundle_dump` 與 `bin/mac/mac_cleanup.sh` 改用 `settings.sh` 之 `REPO_SCRIPTS`
- [x] Phase 5 — 可擴展性 (scalability) @2026-07-09_16:55:00 @Completed @<structural-cleanup>
    - 寫入 `bin/<area>/_lib_*.sh` 慣例至 `bin/README.md` 與 `docs/bin_index.md`
    - 建立 `bin/network/` 區 (含 `scan_network.sh`)
    - `ecosystem.config.js` 確認皆用 `./bin/<area>/<tool>` 全路徑; 註解掉 dead config (port_listenor, file_watcher)
    - `CLAUDE.md` `## 慣例` 加入新工具的加入流程 + 共用 helper 慣例 + 個人 alias 分層
- [x] Phase 6 — 驗證 (verification) @2026-07-09_16:55:00 @Completed @<structural-cleanup>
    - 9 個核心腳本 `bash -n` 語法全通過
    - `bin/system/system_info` 10 個硬體 sub-tool 全部輸出 (驗 bug fix)
    - `bin/mac/disk_analysis-mac.sh` 仍能寫出 markdown 報告
    - `bin/network/scan_network.sh --mode=topology-no-scan` 可用, 產出拓樸報告
    - shellcheck 未安裝於本機, 跳過
    - `git grep 'cmd/'` / `git grep 'project_setup'` / `git grep 'smain'` 殘留僅在歷史歸檔 (CLAUDE.md / README.todo / plans/2026-05-22 ARCHIVED 標頭), **無活躍引用**

## Open Questions

- [x] Q1: `pkg/sysctl/` 與 `pkg/ufw/` 是否仍維護？若無則刪除 (Phase 3.6 / 3.7) @2026-07-09_17:10:00 @Completed
    - `pkg/ufw/` 已不存在 (Phase 3 完成)
    - `pkg/sysctl/` 已搬至 `docs/templates/sysctl/` (`sysctl.conf` / `security/` / `pam.d/`) 為 templates 性質, 不在 `pkg/` 維護
- [x] Q2: `bin/agy-ide_extension_install` 與 `bin/agy-ide_extension_dump` 是否仍指向有效工具？若 `agy-ide` 不存在則整組刪除 @2026-07-09_17:10:00 @Completed
    - 三檔案齊全 (`bin/vscode/agy-ide_extension_install`, `agy-ide_extension_dump`, `agy-ide_extension_list.txt`)
    - 邏輯呼叫 `agy --install-extension` / `--list-extensions`, 為 Antigravity IDE 標準 CLI
    - 與 `CLAUDE.md` 關鍵決策「`run.sh` 同時把 `bin/vscode/*` 連結到 VSCode 與 Antigravity IDE」對接, **保留整組**
- [x] Q3: `bin/backup` 與 `bin/backupSync` 整併為 `bin/backup --mode=diff|sync`？ @2026-07-09_17:10:00 @Completed
    - 已合併為 `bin/backup` (含 `--sync` 模式, 預設為純備份)
    - `bin/backupSync -> backup` 為相容性 symlink (git rm 待下一個 commit 提交)
- [x] Q4: `claudew*` 個人 alias 改放 `~/.bash_local` 個人層級？ @2026-07-09_17:10:00 @Completed
    - 決定敏感 token 存放於 `~/.bash_local`，而 alias 本身預配置於 `bin/bash/.bash_aliases` 中，引用其環境變數
    - `bin/bash/.bash_aliases` 留有註記指向 `docs/notes/bash-local-aliases.md` 範本
- [x] Q5: `bin/scan_target_network` 是否仍使用？若否, 與 `scan_private_network` 一併整合 @2026-07-09_17:10:00 @Completed
    - 內聯 `bin/scan_private_network` (277 行) 與 `bin/scan_target_network` (70 行) 邏輯至 `bin/network/scan_network.sh` 內部 function (`_scan_private` / `_scan_target`)
    - 順帶處理 `bin/scan_devices` (硬體偵測, 與 `system_info` 重疊) 一併 git rm
    - 三個檔案已刪除: `bin/scan_private_network` / `bin/scan_target_network` / `bin/scan_devices`
    - 文件引用全清: `CLAUDE.md` / `README.md` / `README.business.md` / `docs/bin_index.md` / `bin/system/README.md`
- [x] Q6: `skills/bytedance/`, `skills/gdpa/` 是否有人維護？若否, 直接刪除 (Phase 3.8) @2026-07-09_17:10:00 @Completed
    - 整個 `skills/` 目錄已不存在 (Phase 3 完成)
    - `skills/bytedance/` 與 `skills/gdpa/` 為空 stubs, 已移除

## Archive

- [x] Phase 3 — 移除 vendored 與 dead code @2026-07-09_16:37:43 @Completed @<structural-cleanup>
    - 刪除 `bin/git-secret` (vendored, 52KB / 2082 行) — 改用 `brew install git-secret`
    - `pkg/ctags-5.8/` (vendored, 2.2MB / 111 檔) 改為 `git submodule`
    - 刪除 `bin/system/system_performance.sh`, `raspi-config`, `system_service`
    - `tmp/doas-install.sh` → `archive/`
    - `pkg/linux/Linux_kernel_structure.png` + 7 份 `*.md` → `docs/notes/`
    - 刪除 `pkg/ufw/` (整個目錄, 從未被使用)
    - 搬 `pkg/sysctl/` 至 `docs/templates/sysctl/`
    - 移除空目錄 `mac/`, `grafana/`, `inf/`, `log/app/`, `log/rpc/`, `openclaw/yuna/`
    - 移除 `skills/bytedance/`, `skills/gdpa/` (空 stubs)
- [x] Phase 1 — 盤點與文件校正 (truthful docs) @2026-07-09_16:36:58 @Completed @<structural-cleanup>
    - 建立 `bin/README.md` (短索引)
    - 建立 `docs/bin_index.md` (完整索引, 含所有 23 個補列入口)
    - 修改 `README.md` 移除 `cmd/`、`smain`、`bin/project_setup` 敘述
    - 修改 `CLAUDE.md` 移除 Cobra / logrus 依賴；同步目錄結構
    - 改寫 `specs/20260217-fix-agent-symlink.md` 為 `20260217-agent-dir-rename.md`
    - 標 `specs/20260218-monitoring-system-update.md` 為 `OUT_OF_SCOPE` (本 repo 不含 devops)
    - `plans/2026-05-22-*.md`, `plans/2026-05-24-*.md` 標 `## ARCHIVED`
    - 修正 `bin/system/README.md` 標題與內容
